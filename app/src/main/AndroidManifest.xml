<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dreaminreality.ranad_000.worldtour">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GizaDetailsActivity" />
        <activity android:name=".BabylonGardenActivity" />
        <activity android:name=".LighthouseAlexandria" />
        <activity android:name=".TempleArtemis" />
        <activity android:name=".StatueZeus" />
        <activity android:name=".ColossusRhodes" />
        <activity android:name=".MausoleumHalicarnassus" />
        <activity android:name=".GizaPyramid"></activity>
    </application>

</manifest>